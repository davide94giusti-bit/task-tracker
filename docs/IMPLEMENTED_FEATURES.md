# Implemented features

Dashboard urgency cards; Needs Attention; project and workload progress; task table/cards; global search; smart sorting; priority filtering; multi-select bulk actions; full task editor; required and optional checklist items; completion gating; atomic prerequisite creation and checklist conversion; weighted/manual/automatic progress; direct/indirect dependencies and cycle prevention; Dependency load by person with scoring and drill-down; People with function filtering; Projects with linked task creation; month calendar with day details, add-on-day, and task deep links; Today, Upcoming, Completed, and Trash; activity history; local attachments; live reminder center and Windows integration; collapsible macro navigation; themes; backup/import progress and health validation; checksummed backups; merge/replace restore; CSV/XLSX/JSON import preview; CSV/XLSX/JSON/PDF exports; structured readable logs; renderer performance diagnostics; restartable non-Data services; privacy-safe diagnostic ZIP; strict renderer isolation; migrations; Windows installer and portable build configuration.
