import{describe,expect,it}from'vitest';import{readFileSync}from'node:fs';import path from'node:path';const root=path.resolve(import.meta.dirname,'../..'),read=(p:string)=>readFileSync(path.join(root,p),'utf8');
describe('connected PWA',()=>{it('is installable and standalone',()=>{const manifest=JSON.parse(read('apps/connected-web/public/manifest.webmanifest'));expect(manifest.display).toBe('standalone');expect(manifest.icons.some((x:any)=>x.sizes==='512x512')).toBe(true)});it('caches the shell but never API responses in the service worker',()=>{const worker=read('apps/connected-web/public/service-worker.js');expect(worker).toContain("url.pathname.startsWith('/api/')");expect(worker).toContain("caches.match('/index.html')")});it('queues only explicitly safe operations',()=>{const api=read('apps/connected-web/src/api.ts');expect(api).toContain("'/tasks/save'");expect(api).not.toContain("'/backup/import-apply','/tasks/remove'")});it('contains iPhone safe areas and mobile navigation',()=>{expect(read('apps/connected-web/src/styles.css')).toContain('safe-area-inset-bottom');expect(read('apps/connected-web/src/App.tsx')).toContain('BottomNavigation')})});
