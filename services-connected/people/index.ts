import{PersonInput}from'../../packages/contracts';import{authContext,body,call,health,internal,json,withRequest}from'../_shared/runtime';import type{BaseEnv,Fetcher,WorkerHandler}from'../_shared/types';
interface Env extends BaseEnv{DATA:Fetcher}
export default <WorkerHandler<Env>>{async fetch(request,env){return withRequest('connected-people',request,env,async requestId=>{internal(request,env);const url=new URL(request.url),context=authContext(request);if(url.pathname==='/health')return json(health('connected-people',['data']),200,requestId);if(url.pathname==='/list'){const role=url.searchParams.get('role')||undefined;return json(await call(env.DATA,'/rpc',env,context,{method:'POST',body:JSON.stringify({name:'connected_people_workload',args:{p_role:role}})}),200,requestId)}if(url.pathname==='/save'){const person=PersonInput.parse(await body(request)),row={id:person.id||crypto.randomUUID(),full_name:person.fullName,email:person.email||'',company:person.company||'',role:person.role||'',phone:person.phone||'',address:person.address||'',website:person.website||'',preferred_contact:person.preferredContact||'',notes:person.notes||'',tags:person.tags||[]};return json(await call(env.DATA,'/write',env,context,{method:'POST',body:JSON.stringify({table:'people',method:person.id?'patch':'post',id:person.id,row})}),200,requestId)}return json({code:'NOT_FOUND',message:'People route not found',service:'connected-people',requestId,retryable:false},404,requestId)})}};
