import{NotificationPreferences,PushSubscriptionWrite}from'../../packages/connected-contracts';import{reminderEmail}from'../_shared/email';import{sendWebPush}from'../_shared/webpush';import{body,call,health,internal,json,withRequest,workspaceContext}from'../_shared/runtime';import type{BaseEnv,Fetcher,WorkerHandler}from'../_shared/types';
interface Env extends BaseEnv{DATA:Fetcher;RESEND_API_KEY:string;RESEND_FROM:string;APP_URL:string;VAPID_SUBJECT:string;VAPID_PUBLIC_KEY:string;VAPID_PRIVATE_JWK:string;EMAIL_DAILY_CEILING?:string}
async function email(env:Env,to:string,input:{title:string;dateLabel:string;url:string;kind:string},idempotency:string){const template=reminderEmail(input),response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${env.RESEND_API_KEY}`,'Content-Type':'application/json','Idempotency-Key':idempotency},body:JSON.stringify({from:env.RESEND_FROM,to:[to],subject:template.subject,text:template.text,html:template.html})});if(!response.ok)throw Object.assign(new Error(`Email delivery failed (${response.status})`),{permanent:response.status>=400&&response.status<500&&response.status!==429});return response.json()}
export default <WorkerHandler<Env>>{async fetch(request,env){return withRequest('connected-notifications',request,env,async requestId=>{internal(request,env);const url=new URL(request.url);if(url.pathname==='/health')return json(health('connected-notifications',['data','resend','web-push']),200,requestId);if(url.pathname==='/deliver'){const d=await body(request)as any,link=`${env.APP_URL}/?task=${encodeURIComponent(d.taskId)}`,context={userId:'scheduler'},subscriptions=d.pushEnabled&&!d.pushSent?await call(env.DATA,'/admin/subscriptions',env,context,{method:'POST',body:JSON.stringify({workspaceId:d.workspaceId,userId:d.userId})})as any[]:[];let pushSent=!!d.pushSent,emailSent=!!d.emailSent,errorCode:string|undefined;for(const sub of subscriptions){try{const response=await sendWebPush({endpoint:sub.endpoint,keys:{p256dh:sub.p256dh,auth:sub.auth}},{title:'Task Tracker',body:d.title,url:link,tag:d.deliveryId},{subject:env.VAPID_SUBJECT,publicKey:env.VAPID_PUBLIC_KEY,privateJwk:env.VAPID_PRIVATE_JWK});if(response.ok)pushSent=true;else if(response.status===404||response.status===410)await call(env.DATA,'/admin/disable-subscription',env,context,{method:'POST',body:JSON.stringify({id:sub.id,workspaceId:d.workspaceId})})}catch{errorCode='PUSH_FAILED'}}if(d.emailEnabled&&!emailSent&&d.recipientEmail){const count=Number(await call(env.DATA,'/admin/email-count',env,context,{method:'POST',body:'{}'})),ceiling=Number(env.EMAIL_DAILY_CEILING||75);if(count>=ceiling)errorCode='EMAIL_DAILY_CEILING';else try{await email(env,d.recipientEmail,{title:d.title,dateLabel:d.reminderTime||'',url:link,kind:'Reminder'},d.idempotencyKey);emailSent=true}catch(error){errorCode=(error as any).permanent?'EMAIL_PERMANENT':'EMAIL_TRANSIENT'}}const delivered=(!d.pushEnabled||pushSent)&&(!d.emailEnabled||emailSent),attempt=Number(d.attemptCount||1),terminal=attempt>=5||errorCode==='EMAIL_PERMANENT',status=delivered?'delivered':errorCode==='EMAIL_DAILY_CEILING'?'quota_reached':terminal?'failed':'retry',nextAttemptAt=status==='retry'?new Date(Date.now()+Math.min(3600,30*2**attempt)*1000).toISOString():undefined;await call(env.DATA,'/admin/complete-delivery',env,context,{method:'POST',body:JSON.stringify({deliveryId:d.deliveryId,status,emailSent,pushSent,errorCode,nextAttemptAt})});return json({status,emailSent,pushSent},200,requestId)}const context=workspaceContext(request);if(url.pathname==='/subscribe'){const input=PushSubscriptionWrite.parse(await body(request));return json(await call(env.DATA,'/write',env,context,{method:'POST',body:JSON.stringify({table:'push_subscriptions',method:'post',row:{id:crypto.randomUUID(),user_id:context.userId,endpoint:input.endpoint,expiration_time:input.expirationTime,p256dh:input.keys.p256dh,auth:input.keys.auth,device_label:input.deviceLabel}})}),200,requestId)}if(url.pathname==='/preferences'){if(request.method==='GET')return json((await call(env.DATA,'/select',env,context,{method:'POST',body:JSON.stringify({table:'notification_preferences',filters:{user_id:context.userId}})})as any[])[0]||null,200,requestId);const input=NotificationPreferences.parse(await body(request));return json(await call(env.DATA,'/write',env,context,{method:'POST',body:JSON.stringify({table:'notification_preferences',method:'post',row:{id:crypto.randomUUID(),user_id:context.userId,...input}})}),200,requestId)}if(url.pathname==='/test-email'){if(!context.email)throw new Error('No account email is available');return json(await email(env,context.email,{title:'Your Task Tracker email connection works',dateLabel:new Date().toLocaleString(),url:env.APP_URL,kind:'Test email'},`test-${context.userId}-${crypto.randomUUID()}`),200,requestId)}return json({code:'NOT_FOUND',message:'Notification route not found',service:'connected-notifications',requestId,retryable:false},404,requestId)})}};
