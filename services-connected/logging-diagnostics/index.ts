import{authContext,call,health,internal,json,withRequest}from'../_shared/runtime';import type{BaseEnv,Fetcher,WorkerHandler}from'../_shared/types';
interface Env extends BaseEnv{DATA:Fetcher}
export default <WorkerHandler<Env>>{async fetch(request,env){return withRequest('connected-logging-diagnostics',request,env,async requestId=>{internal(request,env);const url=new URL(request.url),context=authContext(request);if(url.pathname==='/health')return json(health('connected-logging-diagnostics',['data']),200,requestId);if(url.pathname==='/list'){const rows=await call(env.DATA,'/select',env,context,{method:'POST',body:JSON.stringify({table:'audit_events',select:'id,created_at,level,service,event_type,request_id,summary'})});return json({services:['gateway','tasks','people','dependencies-progress','reminders','notifications','backup-export','logging-diagnostics','data'],events:rows,privacy:'Tokens, email bodies, push secrets, and credentials are excluded.'},200,requestId)}return json({code:'NOT_FOUND',message:'Diagnostics route not found',service:'connected-logging-diagnostics',requestId,retryable:false},404,requestId)})}};
